** start of main.py **

price = 00
discount = 00

def apply_discount(price, discount):
    
    if not isinstance(price, (int,float)):
        return('The price should be a number')
    elif not isinstance(discount, (int,float)):
        return('The discount should be a number')
    else:
        if price <= 0:
            return('The price should be greater than 0')
        elif discount < 0 or discount > 100:
            return('The discount should be between 0 and 100')
        else:
            discount /= 100
            percentage_off = price * discount
            discounted_price = price - percentage_off
            return(discounted_price)
        

print(apply_discount(50, 0))



# if price <= 0:
#        print('The price should be greater than 0')
#    elif discount <= 0 or discount > 100:
#        print('The discount should be between 0 and 100')
#    else:
#        discount /= 100
#        percent_off_price = price * discount
#        true_price = price - percent_off_price
#        print(true_price)


** end of main.py **

